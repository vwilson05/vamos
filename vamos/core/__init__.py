"""vamos.core — shared building blocks.

Phase 0 placeholder. Future phases land here:
  - client.py    unified ADO REST client (work-items + PRs + git diff)
  - snapshot.py  TeamSnapshot — single WIQL query feeding rule-based agents
  - report.py    Report dataclass + renderers (markdown / Teams / Slack / JSON)
  - delivery.py  Teams + Slack adapters
  - state.py     state/<agent>/<date>.json discipline
"""
