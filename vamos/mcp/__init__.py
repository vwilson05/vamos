"""vamos MCP server — exposes ticket / PR / hygiene actions to Claude via stdio.

Install: `pip install -e '.[mcp]'`
Run: `vamos mcp`
Configure Claude: `vamos mcp install` (prints the snippet to paste).
"""
