"""vamos UI — NiceGUI app.

Entry point: `vamos ui` runs `ui.main:run_app` which boots a NiceGUI server
(uvicorn under the hood). Pages register themselves via @ui.page decorators
in modules under ui/pages/.
"""
